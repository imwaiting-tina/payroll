-- ============================================================
-- 考勤调整字段（调整类型 + 调整金额） — 2026-08-21
-- 调整类型做文本输入，同步显示到详情抽屉；调整金额汇入考勤调整合计。
-- 同时删除旧「特殊调整」入口，改为考勤记录内置字段。
-- ============================================================

ALTER TABLE attendance_records
  ADD COLUMN IF NOT EXISTS adjust_type   VARCHAR(50),       -- 调整类型（文本）
  ADD COLUMN IF NOT EXISTS adjust_amount DECIMAL(12,2) DEFAULT 0;  -- 调整金额（汇入考勤调整合计）
