# 薪酬管理系统 — 技术架构汇报

> 开弈集团多公司薪酬管理系统，替代原有 16 张 Excel 工作表的 Web 应用。

---

## 一、系统概述

本系统是一套完整的 Web 薪酬管理平台，覆盖员工花名册、社保管理、考勤管理、薪资计算、数据总览五大业务模块，支持多公司、按月存储、自动计算个税与社保，并解决国内免 VPN 访问问题。

---

## 二、技术架构总览

系统采用 **前后端分离 + 云服务托管** 的架构，共使用三个云平台：

| 平台 | 承担角色 | 费用 |
|---|---|---|
| GitHub | 代码托管 + 网页托管（GitHub Pages） | 免费 |
| Supabase | 数据库 + 用户认证 | 免费层 |
| 腾讯云 SCF | 云函数反向代理（解决国内访问） | 免费额度 |

---

## 三、前端技术栈

| 项目 | 技术选型 |
|---|---|
| 开发语言 | TypeScript |
| 框架 | React 18 |
| UI 组件库 | Ant Design 5.x |
| 构建工具 | Vite 5 |
| 路由 | React Router 6（Hash 路由） |
| 状态管理 | Zustand |
| HTTP 请求 | Axios |
| 表格/导出 | SheetJS（xlsx） |
| 日期处理 | Day.js |

**前端功能特点：**

- 员工花名册、社保管理（福利套设置 + 员工缴纳明细）、考勤管理、薪资计算、数据总览五大页面
- 编辑采用抽屉（Drawer）交互，不逐格修改，界面整洁
- 社保计算引擎：支持险种开关、基数上下限、四种取整规则、试算预览
- 所有金额统一四舍五入保留两位小数
- 支持 Excel 导入导出（空白模板 / 全量数据）

---

## 四、后端 / 数据层技术栈

| 项目 | 技术选型 |
|---|---|
| 数据库 | Supabase（PostgreSQL 15） |
| 数据访问 | PostgREST（RESTful API 自动生成） |
| 用户认证 | Supabase Auth（JWT） |
| 权限控制 | 行级安全策略（RLS） |
| 云函数 | 腾讯云 SCF（Python） |

**后端技术选型说明：**

本项目采用 Supabase 作为 BaaS（后端即服务），数据库表结构定义后，PostgREST 自动生成 RESTful API，前端直接通过 Axios 调用，无需单独编写后端 CRUD 接口。

另外项目仓库中保留了 FastAPI（Python）+ SQLAlchemy 后端代码，作为计算引擎和扩展接口的备选方案。

---

## 五、数据库设计

核心数据表：

| 表名 | 用途 |
|---|---|
| `company_mapping` | 公司简称对应表（28 家，含开弈中国-美元） |
| `employees` | 员工花名册（主表） |
| `social_welfare_sets` | 社保福利套（费率规则） |
| `housing_fund_sets` | 公积金福利套（费率规则） |
| `employee_welfare_records` | 员工福利缴纳记录（含金额快照） |
| `attendance_records` | 考勤记录（按月） |
| `salary_records` | 薪资记录（按月） |

**关键设计：**

- 员工花名册为数据源头，通过「唯一值」与其他表关联
- 唯一值 = 姓名 + 发薪公司简称 + 入职日期 的 SHA-256 哈希
- 所有数据按月存储（YYYY-MM），历史月份数据冻结，改规则不影响历史
- 计算结果（各险种金额、合计）以快照形式存库，保证历史可追溯

---

## 六、部署架构（解决国内免 VPN 访问）

**问题背景：** 前端托管在境外（Vercel）、数据库在境外（Supabase），国内访问被网络限制，必须挂 VPN。

**解决方案：** 三平台协同，实现国内免 VPN 访问。

```
国内同事浏览器
      ↓ 访问（国内可达）
GitHub Pages（网页托管）→ hro-jay.github.io/PMS
      ↓ 请求数据（国内可达）
腾讯云 SCF 云函数（反向代理）
      ↓ 服务器出境转发（不受网络限制）
Supabase（数据库 + 认证）
```

**部署流程：**

1. 前端代码托管在 GitHub 仓库，构建后通过 GitHub Pages 发布网页
2. 数据库使用 Supabase 托管 PostgreSQL
3. 腾讯云 SCF 部署一个轻量云函数，接收前端请求并转发到 Supabase
4. 前端 API 地址统一指向腾讯云函数 URL

---

## 七、核心业务能力

- **三种计税方式**：正常计税（累计预扣）、劳务计税、不计税
- **社保自动计算**：根据福利套费率 × 基数，自动算出个人/公司各险种金额
- **公积金自动计算**：正常公积金 + 补充公积金，支持独立取整规则
- **按月存档**：每月数据独立存储，合计金额快照留存
- **批量导入导出**：花名册、社保、考勤、薪资均支持 Excel 导入导出
- **数据状态校验**：自动识别基数缺失、规则缺失、不缴纳原因缺失等异常

---

## 七点五、已上线模块功能详情

### （一）员工花名册模块

花名册是整个系统的主数据表，其他模块（社保、考勤、薪资）都通过它关联员工。

**字段（15 项）：** 姓名、状态、成本中心、发薪公司、计税方式、部门、汇报人、职位、职级、考勤制、入职日期、离职日期、唯一值（隐藏）、停用标记、创建时间。

**核心功能：**

1. **发薪公司简称体系** — 内置 28 家公司简称对应表（含「开弈中国」和「开弈中国-美元」两条独立记录），页面显示简称，全称仅作后台备用
2. **六维筛选** — 支持按状态、成本中心、发薪公司、部门、入职日期区间、离职日期区间筛选
3. **编辑抽屉交互** — 点击编辑从右侧拉出抽屉，集中修改所有字段，界面整洁
4. **职级下拉** — 用罗马数字 Ⅰ~Ⅶ 表示，可留空
5. **考勤制下拉** — 全日制 / 非全日制 / 代收代付残疾人 / 不定时工作制 四种
6. **离职自动识别** — 填写离职日期后自动标记为「离职」状态，历史数据留档
7. **入职日期锁定** — 入职日期是唯一值组成部分，录入后不可修改
8. **唯一值机制** — 姓名 + 发薪公司简称 + 入职日期 的 SHA-256 哈希，作为跨表关联键，页面隐藏
9. **批量导入** — 支持简称/全称自动识别、计税方式中文自动转换、增量更新（老员工更新、新员工新增、重复跳过）
10. **批量导出** — 支持导出空白模板和全量数据两种模式
11. **删除功能** — 编辑抽屉底部小按钮 + 确认弹窗，物理删除

### （二）社保管理模块

社保管理分为两个子页面：**福利套设置** 和 **员工福利缴纳明细**。

#### 1. 福利套设置

负责维护社保和公积金的计算规则，分「社保福利套」和「公积金福利套」两个体系。

**社保福利套字段：** 福利套编码（SI-XX）、名称、地区、生效/失效日期、基数上下限、五个险种缴纳开关（养老/医疗/失业/工伤/生育）、个人费率、公司费率、取整规则。

**公积金福利套字段：** 福利套编码（HF-XX）、名称、地区、基数上下限、正常公积金费率、补充公积金（开关 + 费率）、独立取整规则。

**核心功能：**

1. **编码规范** — 社保 SI 前缀、公积金 HF 前缀，如 SI-SH-01、HF-BJ-01
2. **内置不缴纳福利套** — SI-00（不缴纳社保）、HF-00（不缴纳公积金），系统保护不可删除修改
3. **险种级开关** — 每个险种可单独关闭，关闭后自动按 0 计算（如"仅工伤保险"场景）
4. **基数上下限** — 低于下限按下限算，高于上限按上限算
5. **四种取整规则** — 四舍五入、向上取整、向下取整、截位后进位，支持按险种单独配置
6. **试算预览** — 输入测试基数，实时算出各险种个人/公司金额，验证计算逻辑
7. **批量导入导出** — 支持空白模板和全量数据，内置福利套导入时自动拦截

#### 2. 员工福利缴纳明细

负责为每个员工每月匹配福利套、录入基数、自动计算缴纳金额。

**核心功能：**

1. **双福利套绑定** — 每个员工同时绑定一个社保福利套 + 一个公积金福利套
2. **按月管理** — 按薪酬月份（如 2026-08）独立存储
3. **生效/结束日期** — 记录福利套的适用区间
4. **自动计算** — 点「计算」自动根据福利套费率 × 基数算出个人/公司各险种金额和合计
5. **不缴纳场景** — 选择 SI-00 或 HF-00 时，必填不缴纳原因（退休返聘、异地缴纳、其他单位缴纳等）
6. **补充公积金** — 支持开关 + 独立基数
7. **数据状态校验** — 自动识别正常、社保基数缺失、公积金基数缺失、补充公积金基数缺失、不缴纳原因缺失等状态
8. **金额快照** — 计算结果（各险种金额、合计）以快照形式存库，历史月份数据冻结，改规则不影响历史
9. **详情抽屉** — 只读展示员工的完整缴纳明细（基数、各险种个人/公司金额、合计）
10. **删除功能** — 编辑抽屉底部小按钮 + 确认弹窗

---

## 八、成本与运维

| 项目 | 现状 |
|---|---|
| 云服务费用 | ¥0 / 月（三个平台均在免费额度内） |
| 服务器 | 无需自建，全部云服务 |
| 运维 | 无需专职运维，云服务自动维护 |
| 代码管理 | Git 版本控制，GitHub 托管 |

---

## 九、总结

本系统是一套轻量级、零成本的云端薪酬管理方案，通过 React + TypeScript 前端、Supabase 数据库、腾讯云代理的三层架构，实现了：

1. 替代 16 张 Excel 表的人工核算，算薪时间从 2-3 天缩短到 30 分钟内
2. 多公司、按月、可追溯的薪酬数据管理
3. 国内免 VPN 访问，团队协作无障碍
4. 全云端部署，零服务器成本，零运维负担
