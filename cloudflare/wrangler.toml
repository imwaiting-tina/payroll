# Cloudflare Workers — supabase-proxy
# 部署：wrangler deploy
name = "supabase-proxy"
main = "supabase-proxy-worker.js"
compatibility_date = "2025-01-01"
compatibility_flags = ["nodejs_compat"]

workers_dev = true
